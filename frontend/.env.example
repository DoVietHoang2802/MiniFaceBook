# Copy thành .env.local và điền giá trị (file .env* đã gitignore)
# VITE_API_URL=http://localhost:8080/api

# Sentry Frontend (Sprint 6.5) — để trống = tắt
# Tạo project React trên https://sentry.io → Project Settings → Client Keys (DSN)
# VITE_SENTRY_DSN=https://xxxx@o0.ingest.sentry.io/0
