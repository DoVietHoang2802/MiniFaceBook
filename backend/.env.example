# Copy sang env hệ thống / docker-compose — không commit secret thật
# Sentry Backend (Sprint 6.5) — để trống = tắt
# Tạo project Spring Boot / Java trên https://sentry.io → Client Keys (DSN)
# SENTRY_DSN=https://xxxx@o0.ingest.sentry.io/0
# SENTRY_ENVIRONMENT=local
